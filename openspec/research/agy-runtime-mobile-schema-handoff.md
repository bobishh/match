# Match: runtime completion, mobile navigation, typed schema editor

## Latest user correction: direct sync selection and custom workspace sets

This section supersedes the earlier single-workspace invitation UI and library-selection guidance below. Preserve the rest of the runtime/durability work.

- Clicking Sync immediately opens a list of workspace names with checkboxes. The currently active workspace is already selected. Show Generate link on this same screen. No preliminary chooser, Sync workspace button, or extra selection step. Opening this list does not start a network node; generating the invitation starts the necessary transport.
- One selected workspace is the default case. Multiple selected workspaces produce ONE invitation for exactly that fixed set of workspace IDs. Selection of all currently listed workspaces still means a fixed shared set, not enrollment and not subscription to future workspaces. Empty selection disables generation. Show selected names on recipient acceptance.
- Keep Add my device / Sync all as a distinct secondary action for own-device identity enrollment and live personal catalog following. Preserve authentication/approval requirements here; removing UI navigation steps does not remove consent or trust verification.
- Extend OpenSpec and the signed invitation/grant/session contracts consistently for fixed workspace sets. Bind the entire selected set into the authenticated invitation, validate ownership/authority for every selected workspace, issue and verify workspace-specific grants, and authorize every transferred document against that set. Preserve parsing compatibility for existing valid single-workspace invitations. Never interpret a multi-select invitation as all-documents authority. Do not disclose unselected or future workspace metadata. Grant-set acceptance must have explicit failure behavior without falsely claiming complete access.
- Outer BDD: Sync opens selection with active workspace checked; Generate creates usable single-workspace invite without extra navigation; selecting A+B transfers A+B but excludes C and future D; empty selection and invalid/expired grant set fail truthfully; enrollment stays distinct. Check mobile layouts and keyboard/focus behavior.
- Implement our OWN domain-specific schema/AST editor. No dependency research detour and no imported generic JSON form-builder as the primary UX. A maintained text-editing/highlighting primitive may be reused if needed. Typed JSON text and structural tree share one draft; custom nodes/property editors/type validation understand Match IDs, parent-child relationships, fields, options and soft deletion. Preserve the existing validation/atomic Apply/conflict requirements below. This remains schema editing, not an executable DSL or a second state authority.

Execute this task now in /Users/bogdan/Workspace/personal/alcoholics_audacious/match. User selected Gemini 3.8 Flash with high thinking and explicitly authorized --dangerously-skip-permissions. Work autonomously on implementation and verification. Do not touch Emacs or unrelated personal apps. Preserve dirty changes, including legacy migration fixes and any newer repairs. Read applicable AGENTS.md, GEMINI.md, README.md and the existing OpenSpec before editing.

## Outcome and workflow

URGENT USER CORRECTION: Rejected was an existing column and has disappeared. Treat this as a regression to restore, not a new feature proposal. First inspect prior code/history and current preset/migration/projection to recover original Rejected behavior and existing cards/IDs; write a regression scenario and repair it before the larger remaining work. Do not silently map rejected to Archive/Trash or drop rejected cards. The user's immediate complaint is the missing column, not a request for a mandatory rejection questionnaire. Optional retrospective notes are secondary.

Finish the workspace-kernel-and-scoped-sync change with real runtime integration, then repair mobile navigation and implement a typed JSON / visual AST schema editor. User also wants a Rejected column in Job search with an optional rejection reason / retrospective note. Update OpenSpec to capture these newly authorized UI requirements; preserve existing security, durability and compatibility guarantees.

The prior review is a list of findings to recheck against current files, not an instruction to redo repairs already completed. Existing green tests and checked tasks alone are not evidence. Reopen unsupported checkboxes. Do not weaken requirements or manufacture success states. Keep a concise evidence record for each gate: actual invariant, initially failing test, implementation, passing commands, remaining gaps. Continue beyond diagnosis until the requested implementation is complete.

## A. Validation and atomic persistence

Ensure application source is actually typechecked: npx vue-tsc -p tsconfig.app.json --noEmit. The earlier root vue-tsc --noEmit skipped application checking. Fix missing model exports and broken APIs; do not suppress source errors. Dependency-only skipLibCheck is acceptable if necessary.

Replace the former Map + multiple localStorage writes with IndexedDB transactions for immutable per-document/per-change-hash bytes, proofs, command receipts and relevant metadata. Publish and notify replication only after transaction completion. Snapshot is a rebuildable acceleration and must cover known heads; it must not replace unseen changes. A crash between a durable change and optional snapshot must recover correctly. Failure must never be swallowed into apparent durability. Existing persisted formats require a non-destructive migration.

Prove: mid-transaction abort leaves no partial records; reload retains prior state; identical transaction-ID retry returns same receipt; different command with same ID fails; two genuinely isolated stale tabs preserve both edits; compaction preserves unseen writer; deleting disposable JSON leaves history reconstructable. Serialize overlapping same-document commands through the real publication boundary.

## B. Identity, enrollment, admission and proofs

Persist real identities/keys and prevent concurrent first-open tabs from creating competing profiles. Enrollment retains target device keypair and installs issuer person identity plus correct target certificate only after verified approval. Resolve fresh enrollment versus existing unrelated profile explicitly: a new enrollment target must not first bootstrap an unrelated permanent person and then silently overwrite it. Existing different profile yields identity_conflict with no data loss. Never send root/workspace metadata before approval. Remove invented guest IDs and fallback credentials; a legitimate migrated workspace ID named default is not itself invalid.

Wire AdmissionController into every inbound runtime path. Verify actor binding, actual change hash/document, device certificate chain, editor's own person identity and workspace authority/grant, signatures and allowed writes. Isolate pending candidates from trusted documents, snapshots, UI and outbound messages. Missing proof remains pending until dependencies arrive; rejected changes never publish or receive false durable acknowledgement. Valid editor grants must work for a different person, not require that person's certificate to chain to workspace owner's identity. Relay preserves original author attribution.

Use one coherent proof repository: local command proofs, actor bindings/certificates, export, admission and replication must share persisted proof dependencies. Export excludes keys/root/invite secrets and supports real restore/import by workspace ID without merging unrelated documents into active workspace.

## C. Scoped runtime replication

Replace single-active-workspace byte callbacks with repository APIs by document ID, authorized document listing, proof access, durable admitted changes and catalog/document subscriptions. Sync all must replicate private personal root and both existing and future referenced workspaces to enrolled owner devices. Workspace join must load exactly the selected workspace even if another is active, retain recipient person identity, persist verified editor grant and workspace reference, and disclose no unrelated IDs/titles/root/history/proofs.

Use native Automerge per-peer/per-document sync state end to end. No full-document fallback on protocol/admission errors; null sync message means quiescence. Generate protocol replies until convergence, without inventing business changes or echo loops. Do not serialize whole documents for each live edit just to bridge session APIs. Initial transfer, later deltas, reconnect, proof exchange, bounded framing/backpressure and actual file chunking/progress must run through production transport paths. Persistent trust and discovery/reconnect must work after reload. Do not report all files complete while unavailable/pending blobs remain.

Remove insecure shared enrollment BroadcastChannel or development HTTP bus shortcuts that accept arbitrary approval messages without matching invitation, recipient, signatures and scope. A Vite-only in-memory /api/sync-signal bus cannot prove production behavior. Normal UI may show connected/enrolled only after actual verified durable success; rejected/cancelled/expired/network failures need truthful states and cleanup. Source must continue accepting connections needed after enrollment; avoid one-shot acceptor dead ends.

Test real isolated profiles with A/J and A/K owner devices and B/L guest: two workspaces reach K, only selected workspace reaches L, future private workspace reaches K without disclosing metadata to L. Bidirectional edits and reload must work. Tampering, pending proof, expired invite and denied enrollment must not mutate trusted state. Measure first transfer versus one edit and reconnect through actual session path. Existing tests that only assert Connected, Device enrolled or a Sync button must be replaced/extended to prove contents, isolation, identities and durable usability.

## D. Mobile navigation and links

Mobile UI currently mixes board controls, navigation, settings and links in a crowded header. Design mobile-first: workspace switcher + board content + accessible hamburger button. Drawer/menu houses Sync, import/export, schema editor/settings, Trash, Needs placement and help/agent guide in clearly labeled groups. Card/column actions stay contextual. Preserve direct desktop usability and preset document/template/artifact flows. Inspect links for wrong destinations, labels, duplicated navigation and old domains. Verify actual project destinations before changing hosts; do not replace unrelated external user-content URLs or invent endpoints.

Drawer needs accessible label, focus management, Escape/backdrop close, and restoration to trigger. At 360/390/430 px, controls must remain reachable, no accidental whole-page horizontal overflow, dialog content scrolls within viewport and keyboard does not hide essential actions. Board horizontal scrolling is intentional; preserve usable column snapping. Verify 1024 px too.

## E. Typed JSON / visual AST schema editor

Replace a collection of Add field / Remove field forms as the primary schema-management experience with one schema editor: typed JSON text and a visual structural tree editing the same draft. Prefer a maintained small editor library if suitable; inspect existing dependencies and current official documentation before adopting one. Choose and implement a practical solution; do not stop at library research or build a general programming language.

The editor covers board schema, column definitions/order, field definitions/types and select options. It must not expose private keys, trust grants or arbitrary workspace data as schema. This is schema editing, not a declarative UI-rendering DSL and not a second CRDT/state authority. Store canonical schema using the existing typed domain entities; project a disposable typed JSON draft for text/tree editing. Switching views must preserve the draft. Provide type-aware completion or constrained tree node types, validation with precise JSON paths, and clear pending/error state. Mobile uses tree/text tabs rather than cramped split panes.

Apply validates the entire draft and previews meaningful schema changes, then translates them through a single atomic domain command boundary. Never replace root entity maps or replay a parallel event log. Existing IDs stay stable on rename/reorder. Select values keep referencing option IDs. Removals mean soft deletion with retained relationships and values. Avoid silently converting omission to destructive deletion: distinguish omission/removal explicitly and show the result before Apply. Invalid changes make no durable partial writes. Cancel discards draft. Reopen/reload reflects committed schema. If concurrent edits arrived while draft was open, report/reconcile conflict explicitly instead of overwriting stale state. Never regenerate existing IDs just because JSON was edited.

## F. Rejected in Job search

Add/restore Rejected as an ordinary configurable column in the Job search preset with stable column ID/binding, and an optional rejection reason / retrospective note (what went wrong). Rejected is a business state, separate from Archive and Trash. Retain task data, documents, timestamps, original history and relationships; no automatic deletion. Generic workspaces should not acquire job-specific fields. Migrate existing rejected data without dropping cards or confusing it with deleted/bin data; avoid re-adding a deliberately removed column on every load. Reason can be absent or unknown; never invent feedback. Make the preset schema editable through the same editor.

## Verification and handoff

Outer BDD first for UI: write real-route Given/When/Then scenarios before implementation and verify happy plus failure/pending paths. Use a free alternate port without killing user servers. Specifically cover mobile menu navigation/focus and failed sync, schema rename/select-ID retention/apply/reload plus invalid JSON/type/concurrent-draft failures, soft-deleted column hide/restore with separately deleted child retained as deleted, and moving a lead to Rejected with optional note/reload.

Run application typecheck, relevant focused tests, full regression tests, production build, real-route Playwright scenarios and strict OpenSpec validation. Do not assert tests were observed failing unless actually run. Report any unresolved requirement honestly, leaving its gate unchecked. Do not deploy partial repairs as part of this handoff; finish local work and produce exact verification evidence for review.
