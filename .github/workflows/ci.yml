name: Verify

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/checkout@v7
        with:
          repository: bobishh/meta-mesh
          ref: ef5e5da5033da92e5a39169b3ee353828fa9ac5d
          path: vendor/meta-mesh
          ssh-key: ${{ secrets.META_MESH_DEPLOY_KEY }}
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run verify:meta-mesh
      - run: npm run build
      - run: npm test

  browser-core:
    name: Browser (core)
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/checkout@v7
        with:
          repository: bobishh/meta-mesh
          ref: ef5e5da5033da92e5a39169b3ee353828fa9ac5d
          path: vendor/meta-mesh
          ssh-key: ${{ secrets.META_MESH_DEPLOY_KEY }}
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run verify:meta-mesh
      - run: npx playwright install --with-deps chromium
      - run: npx playwright test --project=core

  browser-network:
    name: Browser (${{ matrix.project }})
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      # Iroh relay sessions can linger after Chromium exits. Serial projects keep
      # remote relay traffic isolated; each project still gets a fresh runner.
      max-parallel: 1
      matrix:
        project:
          - chat
          - durable-mesh
          - sync
          - workspace-roles
          - workspace-sync-data
          - scoped-sync
          - scoped-enrollment
          - scoped-existing-board
          - scoped-existing-identity
          - scoped-delegated-owner
          - succession-named
          - succession-quorum
    steps:
      - uses: actions/checkout@v7
      - uses: actions/checkout@v7
        with:
          repository: bobishh/meta-mesh
          ref: ef5e5da5033da92e5a39169b3ee353828fa9ac5d
          path: vendor/meta-mesh
          ssh-key: ${{ secrets.META_MESH_DEPLOY_KEY }}
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run verify:meta-mesh
      - run: npx playwright install --with-deps chromium
      - run: npx playwright test --project=${{ matrix.project }}
